# TEDE example dataset config — copy and edit for your project.
# Format: standard YOLO labels (class_id cx cy w h, normalised to [0, 1]).

path: ../data
train: images/train
val: images/val
test: images/test

nc: 3
names:
  0: person
  1: vehicle
  2: animal
